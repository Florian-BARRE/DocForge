# Code Reviewer — Memory Index

- [Pipeline engine edge-selection](pipeline-engine-edge-selection.md) — UPDATED: engine now RANKS out-edges (ScoreBelow>WhenEquals>OnSuccess/OnFailure>Always) then takes first match; cross-rank ambiguity FIXED, SAME-rank fan-out still order-dependent — verify the validator rejects equal-rank duplicates.
- [Metagen + embed node traps](metagen-embed-node-traps.md) — S6/S7 latent traps: chunk-metagen REPLACES generated_meta on chaining (not UNIQUE_IN_GRAPH), duplicate targets silently dedupe, datetime schema hint dropped, cross-scope→silent zero; F4 openai_compat factory = 7 consumers (PIPELINE.md says 6). MI/sparse/httpx/strip/describe all verified-SAFE.
- [describe() reflection fragility](describe-reflection-fragility.md) — node.describe() uses `field_info.annotation.__name__`; crashes on `X | None`/union slots, mangles `list[X]`. Palette endpoint calls it for every node, so one bad slot 500s the whole palette.
- [DB-layer review heuristics](db-layer-review-heuristics.md) — FK-only store pitfalls: self-FK insert batching (>1000 rows breaks, use deferrable), unindexed FKs w/ ondelete, value_enum binding, Qdrant datetime-range gap + slug collisions, update(None) can't reset NULL.
- [Enrich trace & failure traps](enrich-trace-and-failure-traps.md) — byte-carrying artefacts (FigureItem.image/IR crops) blow up the execution-record ~8x (per-node model_dump at every hop; foreach items_count fix is partial); enrich_apply mutates run_input.ir in place (output.ir IS input); one flaky figure fails the WHOLE document (default ERROR_POLICY.FAIL + loud foreach). FromFirst validates/resolves correctly.
- [ForEach primitive traps](foreach-primitive-traps.md) — docforge-rework/shared engine: non-Artifact foreach terminal passes validation then crashes uncaught (item_type accepts scalars); engine catches only ResolutionError not pydantic ValidationError; no ge=1 guard on direct ForEach ctor (0→deadlock); progress events can't attribute concurrent items; foreach record.output duplicates every item value.
- [Contextualize per-chunk-LLM traps](contextualize-llm-perchunk-traps.md) — llm_context (the shape metagen will copy): O(n²) doc-view rebuild per chunk (FULL/SECTION), unconditional whitespace flattening even under the cap, keep_raw try wraps view-build (masks real bugs), ChatOpenAI 4th ad-hoc copy + "unused" literal ×4 → recommend a leaf OpenAIClientFactory. All non-blocking; check on any new per-chunk/per-item LLM node.
- [Rework stage-layer vision](rework-stage-layer-vision.md) — post-2026-07-05: stages/ is the center of gravity, graph engine = substrate only. What's UI-dead vs advanced-only (SWITCH_FIELDS/scored/recipes/mechanics/artefacts/explored), SchemaForm trio misplaced in unrouted pipeline-editor (breaks stage-rail if deleted naively), StageViewResponse double-wrap, edit/topology is the only substrate dep of edit/.
