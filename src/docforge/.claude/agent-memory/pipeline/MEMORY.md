# Pipeline Agent Memory — Index

- [R2 stage restructure pattern](r2-stage-restructure-pattern.md) — how to decompose a legacy sN_* stage into native steps under ingest/stages/<name>/, relocate its result type, repoint importers, delete legacy; gotchas (fingerprint override, markdown fp key, PyMuPDF in tests).
