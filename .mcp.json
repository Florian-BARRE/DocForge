{
  "mcpServers": {
    "docforge": {
      "command": "python",
      "args": ["src/docforge_mcp/entrypoint.py"],
      "env": {
        "MCP_TRANSPORT": "stdio",
        "DOCFORGE_API_URL": "http://localhost:10020"
      },
      "description": "DocForge document intelligence — full REST surface as MCP tools (stdio). For remote clients, run the `mcp` compose service and connect to http://<host>:10030/mcp with an Authorization: Bearer <MCP_AUTH_TOKEN> header."
    },
    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"],
      "env": {
        "MEMORY_FILE_PATH": ".claude/knowledge-graph.json"
      },
      "description": "Persistent knowledge graph — DocForge decisions, patterns, entities, relations"
    },
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp@latest"],
      "description": "Up-to-date library docs — FastAPI, SQLAlchemy, Qdrant, Pydantic, arq, BGE-M3, React"
    },
    "deepwiki": {
      "command": "npx",
      "args": ["-y", "deepwiki-mcp@latest"],
      "description": "Deep understanding of any GitHub repository"
    }
  }
}
