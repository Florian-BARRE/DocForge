# ─── Gotenberg 8 — runtime tuning template ──────────────────────────────────────
# Copy to `services/gotenberg/.env`. Defaults are fine for dev; the file is kept so
# the docker-compose `env_file` directive does not error. Add overrides below if needed.
