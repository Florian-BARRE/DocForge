# ─── Redis — environment template ───────────────────────────────────────────────
# Copy to `services/redis/.env`. No env vars are needed for the default image; the
# file is kept so the docker-compose `env_file` directive does not error.
